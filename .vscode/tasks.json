{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Cygwin: make",
      "type": "shell",
      "command": "make",
      "args": [],
      "problemMatcher": "$gcc",
      "group": { "kind": "build", "isDefault": true },
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PATH": "C:\\cygwin64\\bin;${env:PATH}"
        }
      }
    },
    {
      "label": "Cygwin: make clean",
      "type": "shell",
      "command": "make",
      "args": ["clean"],
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PATH": "C:\\cygwin64\\bin;${env:PATH}"
        }
      }
    }
  ]
}
